using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnePixelProductionClass// : MonoBehaviour
{
    uint CharacterID = 0;

    ushort RColor = 0;
    ushort GColor = 0;
    ushort BColor = 0;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
